int ComputeFactorial(int num);

int main(int argc, char[][] argv) {
    print(ComputeFactorial(5));
    return 0;
}

int ComputeFactorial(int num) {
    int aux;

    if (num < 1)
        aux = 1;
    else
        aux = num * (ComputeFactorial(num - 1));

    return aux;
}