int factorial(int n);

/* The output on stdout should be 120
 */
int main(int argc, char[][] argv) {
    print(factorial(5));
    return 0;
}

int factorial(int n) {
    int x;

    if (n < 1)
        x = 1;
    else
        x = n * factorial(n - 1);

    return x;
}