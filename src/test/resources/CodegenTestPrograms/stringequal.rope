boolean stringequal(char[] s1, char[] s2);
int stringlength(char[] s);

/* The output on stdout should be True or False depending on the args passed in
 */
int main(int argc, char[][] argv) {
    print(stringequal(argv[1], argv[2]));
    return 0;
}

boolean stringequal(char[] s1, char[] s2) {
    int n;
    int m;
    int i;
    boolean x;

    n = stringlength(s1);
    m = stringlength(s2);

    if (m == n) {
        x = true;
        i = 0;
        while (x && i < n) {
            if (s1[i] != s2[i])
                x = false;
            else {
                // Do nothing
            }
            i = i + 1;
        }
    } else {
        x = false;
    }

    return x;
}

int stringlength(char[] s) {
    int i;

    i = 0;
    while (s[i] != '\0') {
        i = i + 1;
    }

    return i;
}