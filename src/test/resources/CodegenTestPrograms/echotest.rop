/* The output on stdout should be the repeat of everything fed as an argument
 */
int main(int argc, char[][] argv) {
    int i;

    i = 0;
    while (i < argc) {
        print(argv[i]);
        i = i + 1;
    }

    return 0;
}